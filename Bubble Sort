The Second Sorting Algorithm is Bubble Sort

this is the best sorting algorithm for larger datasets

best case is O(n)
average and worst case are same O(n^2)
space complexity is O(1)

un stable state

Code::

#include<iostream>
using namespace std;
void BubbleSort(int Arr[],int n){
  for(int i = 0 ; i<n ; i++){
    for(int j = 0 ; j<n-i-1 ; j++){
      if(Arr[j]>Arr[j+1]){
        int temp = Arr[j];
        Arr[j] = Arr[j+1];
        Arr[j+1] = temp;
      }
    }  
  }
}
int main(){
  int n;
  cout<<"Enter the Size of Array: ";
  cin>>n;
  int Arr[n];
  cout<<"Enter The array: ";
  for(int i = 0 ; i< n ;i++){
    cin>>Arr[i];
  }
  cout<<endl;
  cout<<"Sorted Array is: ";
  BubbleSort(Arr,n);
  for(int i = 0 ; i< n ;i++){
    cout<<Arr[i]<<" ";
}
}


